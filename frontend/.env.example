# Backend API URL
# In development: leave empty — the Vite proxy (vite.config.js) handles /api/* → localhost:5000
# In production (Render/Netlify): set this to your deployed backend URL
VITE_API_URL=https://news-portal-backend-mv9p.onrender.com
