# Backend API URL
# In development, the Vite proxy handles /api/* requests, so this is mainly for reference.
# In production, set this to your deployed backend URL (e.g., https://api.yourdomain.com)
VITE_API_URL=http://localhost:5000
